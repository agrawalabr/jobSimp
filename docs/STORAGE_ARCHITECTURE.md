# Storage architecture (superseded)

This document described the old `jobsimp-entities` / `store.js` design (deleted).

**Current source of truth:** [`DATABASE.md`](./DATABASE.md) — IndexedDB `jobsimp-graph` with per-type object stores (`user`, `profile`, `resume`, `graph`, `job`, …) via [`src/dao/`](../src/dao/).

Onboarding flow: [`ONBOARDING_WORKFLOW.md`](./ONBOARDING_WORKFLOW.md).
