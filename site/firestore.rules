rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Client SDK: deny all. Beacon API uses Admin SDK (bypasses rules).
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
